#include <stdio.h>

/**
 * main - Print numbers from 0-9
 *
 * Return: (0) successful execution
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}

	printf("\n");
	return (0);
}

